#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void dfs(vector<vector<int>> &adj){
    for(auto num : adj){

    }
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int casos;
    cin >> casos;
    while(casos--){
        int qtd;
        cin >> qtd;
        vector<vector<int>> adj(qtd);
        for(int i = 1;i <= qtd;i++){
            int ent;
            cin >> ent;

            //percorrer com dfs

        }
    } 
}
#include <bits/stdc++.h>
using namespace std;
int main(){
    int tamanho;
    while (cin >> tamanho && (tamanho != 0))
    {
        int elementos;
        while (cin >> elementos && (elementos != 0))
        {
            stack<int> pavilhao;
            int contador = 1;
            if (contador == tamanho){
                cout << "yes" << '\n';
                break;
            }else if(elementos == contador){
                contador ++;
            }else if()
        }
        
    }
    
}